<section class="relative h-[20vh] md:h-[30vh] sm:h-[25vh] bg-center bg-cover flex flex-col justify-center items-center text-white text-center px-4" style="background-image: url('<%= asset_path('hero.jpeg') %>');">
  <div class="absolute inset-0 bg-black opacity-50"></div>
  <!-- Top left title -->
  <div class="absolute top-4 left-4 text-2xl md:text-3xl font-bold">Rent A Clue!</div>

  <!-- Center content -->
  <div class="relative z-10">
    <h1 class="text-4xl md:text-5xl font-extrabold">Puzzle Catalog</h1>
  </div>
</section>

<div class="max-w-7xl mx-auto py-8 px-4">
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <%= render @puzzles, { small: false } %>
  </div>
</div>
