<%= form_with(model: inventory, url: inventory.new_record? ? admin_puzzle_inventory_index_path(@puzzle) : admin_puzzle_inventory_path(inventory)) do |form| %>
  <% if inventory.errors.any? %>
    <div class="bg-red-100 text-red-700 border border-red-300 p-4 rounded">
      <h2 class="font-semibold">
        <%= pluralize(inventory.errors.count, "error") %> prohibited this inventory from being saved:
      </h2>
      <ul class="mt-2 space-y-1">
        <% inventory.errors.full_messages.each do |message| %>
          <li>• <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :price_bought_for, "Price bought for", class: "block text-sm font-medium text-gray-700" %>
    <%= form.text_field :price_bought_for, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
  </div>

  <div>
    <%= form.label :condition, "Condition", class: "block text-sm font-medium text-gray-700" %>
    <%= form.text_field :condition, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
  </div>

  <div>
    <%= form.label :details, "Details", class: "block text-sm font-medium text-gray-700" %>
    <%= form.text_field :details, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
  </div>

  <div>
    <%= form.submit "Save Inventory", class: "w-full bg-blue-600 text-white py-2 px-4 rounded-md shadow hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2" %>
  </div>
<% end %>
